class_name GuardPost
extends Node2D
## Marks a settlement entrance/perimeter position a survivor can guard.
## Turned into a standing Job by Settlement.setup_jobs() so the job board's
## normal claim/release rules keep two survivors from stacking on one post.
